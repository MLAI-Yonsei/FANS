python experiments/experiment_script.py --model <MODEL_NAME> [OPTIONS]

--nodes           # 처리할 노드 수 (기본: 10 20 30 40 50)
--gpu             # GPU ID (-1=CPU, 기본: -1)
--output_dir      # 결과 저장 디렉토리 (기본: 자동 생성)
--config_type     # DAG 타입 필터 (ER, SF, all, 기본: all)
--dataset_indices # 처리할 데이터셋 범위 (예: "0-5")


python experiment_script.py --model iscan --gpu 0
python experiment_script.py --model iscan --nodes 10 --config_type ER --gpu 0
python experiment_script.py --model iscan --config_type ER
